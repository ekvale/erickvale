from django.apps import AppConfig


class HtacConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "htac"
    verbose_name = "HTAC — Health Trends Across Communities"
